import SwiftUI

struct SearchView: View {
    var body: some View {
        Text("Search")
            .font(.largeTitle)
            .bold()
    }
}

struct SearchView_Previews: PreviewProvider {
    static var previews: some View {
        SearchView()
    }
}