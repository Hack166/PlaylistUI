import SwiftUI

struct NewView: View {
    var body: some View {
        Text("New")
            .font(.largeTitle)
            .bold()
    }
}

struct NewView_Previews: PreviewProvider {
    static var previews: some View {
        NewView()
    }
}
